{
  "name": "sk-health-club",
  "version": "2.0.0",
  "description": "SK Health Club — Premium Fitness Destination Pune",
  "scripts": {
    "dev": "npx tailwindcss -i ./css/styles.css -o ./css/main.css --watch",
    "build": "npx tailwindcss -i ./css/styles.css -o ./css/main.css --minify",
    "start": "live-server --port=3000 --host=localhost"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "live-server": "^1.2.2",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.14",
    "sharp": "^0.35.3",
    "swiper": "^11.0.0"
  }
}
